using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AngerGameMaster : MonoBehaviour
{
    public GameObject Gate;

    //private ExamineTrigger GateTrigger;

    void Awake()
    {
        //GateTrigger = Gate.GetComponent<ExamineTrigger>();
        //GateTrigger.setMasterAllowance(true);
    }

    // Update is called once per frame
    void Update()
    {
    }
}
